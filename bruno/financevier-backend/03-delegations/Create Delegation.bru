meta {
  name: Create Delegation
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/delegations
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "email": "delegated@example.com",
    "role": "SUPERVISOR"
  }
}
